---
tags: [difly, 穿線服務, 新專案]
created: 2026-08-18
---

# 穿線服務（新專案）

> 羽拍穿線服務的管理系統，獨立於球場預約與租拍。完整交接文件：`/Users/defi/Desktop/projects/code/stringing/HANDOFF.md`

## 一句話

客人拍線斷了 → 到店 kiosk 選線種＋磅數 → 印貼紙貼拍 → 放智慧格口 → 員工取走穿線 → 穿好送回 → 客人付款 → LINE 通知取件 → 掃碼開格取拍。

## 流程（7 步，含付款）

1. 客人交拍：kiosk 選線種＋磅數 → 印 4×3cm 標籤（無 QR，見「列印」段）→ 開空格放拍（待收件）
2. LINE 通知員工新單
3. 員工取件（手機後台開格）→ 格釋放（穿線中）
4. 穿好送回：開「新空格」放拍（待取件）
5. 客人付款（臨櫃/Line Pay/匯款）→ 員工標「已付款」
6. LINE 通知客人取件（**付款後才發**）
7. 客人掃碼開格取拍（已完成）

**核心**：格子是流動的（交拍格 ≠ 取件格），動態分配。

## 線種價目表（11 條）

| 型號 | 線徑 | 磅數上限 | 價格 |
|------|------|---------|------|
| AL-69 | 0.69mm | 28 | 250 |
| AL-66 | 0.66mm | 30 | 250 |
| AL-65 | 0.65mm | 30 | 300 |
| AL-63 | 0.63mm(鍍鈦) | 28 | 350 |
| YOUNG66 | 0.66mm | 30 | 250 |
| YOUNG65 | 0.65mm | 28 | 300 |
| YOUNG63 | 0.63mm(鍍鈦) | 28 | 350 |
| KIZUNA Z61 | 0.61mm | 28 | 450 |
| KIZUNA Z63X | 0.63mm | 28 | 400 |
| KIZUNA Z65X | 0.65mm | 28 | 400 |
| DEARFLY61 螺紋線 | 0.61mm | 30 | 350 |

定價＝線種決定；磅數上限＝線種決定（kiosk 要限制）。

## 硬體

- kiosk 主機：Win10（HJS，i7/8G/256G）
- 印表機：**GPRINTER GP-3120TN**（203dpi，USB，Win10 驅動＝Seagull 硒谷驅動）
- 標籤：**4×3cm 直式**（明確無 QR——QR 需求已移除）
- 鎖控板：RS-485（現有）

## 列印（中文解法，已定稿）

> ⚠️ **這台 GP-3120TN 的坑**：TSPL 原生為**單字節字型 FONT 0~8**（無內建中文字）、**不吃 BITMAP 指令**（實測最小方塊也無反應）。所以 raw TSPL 印中文＝亂碼/空白，**不可行**。
>
> ✅ **可行解法＝Seagull 驅動印中文**（kiosk 已裝 Seagull 驅動，用 Windows 中文字型「微軟正黑」渲染再送印表機）：
> - 腳本：`code/smartlocker/web/scripts/print-label.ps1`
> - 用 `System.Drawing.Printing.PrintDocument` 畫４行（**線種+色 / 磅數+店名英文 / 金額+店名中文 / 取件號**），紙張用驅動內建 `40 mm x 30 mm`（不是自訂，否則字會轉 90°＋溢頁）
> - `$` 坑：PowerShell 字串 `"NT$250"` 的 `$2` 會被當變數展開成空字串，**要用單引號 `'NT$250'`**
> - **換文字＝改腳本參數** `店名/店名英文/線種/磅數/金額/取件號` 再跑一次（這就是你的「editor」，不需 GUI）
> - 繁體全覆蓋：用微軟正黑，`太平永成店`/`長壽店`(長/壽) 任何店名都印得出
>
> **✅ 自動帶（接訂單資料，已做＋已驗證 2026-08-23）**：
> - `print-label.ps1` 支援 `-ConfigFile <json>`（讀 JSON 欄位 `store/storeEn/line1/line2/line3/line4/printer`），供程式安全傳中文/`$`（避免 CLI 轉義）
> - **kiosk-print-poller.ps1**（**PowerShell 版，kiosk 無 node 可跑**）：輪詢 `/api/print-jobs?status=pending` → 用 `job.label`（`model`+`color`→線種、`tension`→磅數、`price`→金額、`pickupCode`→取件號）自動組 4 行 → 寫暫存 JSON → 呼叫 `print-label.ps1 -ConfigFile` → 開格 → 回報完成；`-Once`＝單步驗證
> - **常駐**：kiosk 用 `schtasks` 建「KioskPrintPoller」排程任務（onlogon），跑 `launch-poller.cmd` → 輪詢導向 `poller.log`；每 3 秒輪詢、有新綁定單就自動印
> - 店名從環境變數 `STORE`/`STORE_EN` 帶入（每店 kiosk 各設自己的）；`PRINTER` 指定印表機
> - ⚠️ **UTF8 坑（重要）**：PowerShell `Invoke-RestMethod` 抓 API 把中文（如 color「白」）**誤解碼成亂碼（ç½）**。**必須用 `HttpWebRequest` + `StreamReader(UTF8)` 讀流**解碼，否則標籤中文色會爛掉。

## 架構

- 雲端（Vercel+Neon）：訂單、員工手機後台、LINE 通知、kiosk 網頁
- 本地 kiosk（Win10）：列印＋格口控制（輪詢雲端指令）

## 訂單狀態機

`待收件 → 穿線中 → 待取件 → 已完成`（＋付款旗標）

## 估時

約 **5–9 工作天**；剩最大變數在 **RS-485 格口整合**（列印已定稿，見「列印」段）。

## 相關

- [[首頁索引]]
- [[06-LINE整合]]
- [[11-待辦與下一步]]
